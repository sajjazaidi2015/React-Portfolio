import React from 'react' 

function Portfolio() {
    const message = "You are on Portfolio page"

    return (
        <div className="container">
          <h2>{message}</h2>
        </div>
      );
}

export default Portfolio;