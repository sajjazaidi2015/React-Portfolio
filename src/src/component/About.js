import React from 'react' 

function About() {
    const message = "You are on about page"

    return (
        <div className="container">
          <h2>{message}</h2>
        </div>
      );
}

export default About;