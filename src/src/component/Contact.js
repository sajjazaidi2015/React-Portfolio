import React from 'react' 

function Contact() {
    const message = "You are on Contact page"

    return (
        <div className="container">
          <h2>{message}</h2>
        </div>
      );
}

export default Contact;