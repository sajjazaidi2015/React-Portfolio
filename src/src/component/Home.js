import React from 'react' 

function Home() {
    const message = "hello world"

    return (
        <div className="container">
          <h2>{message}</h2>
        </div>
      );
}

export default Home;